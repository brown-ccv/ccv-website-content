# Schema for all yml files in our-work/collaborations 
title: str()
description: str()
groups: list(include('group'))
tags: list(include('tag'))
people: list(include('person'))
links: list(include('link'), required=False)
---
language: regex('^(python|julia|R|matlab|unix-cli|rust)$')
tag: regex('^(behavioral-tasks|neuro-imaging|neuro-engineering|computational-psychiatry)$')
#people involved in the project
person:
  name: str()
  github_user: str(required=False)
# These are links associated with the software 
link:
  # Must match one of the values in the regex
  category: regex('^(website)$')
  url: str()
  

  